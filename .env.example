# ─── TARS Chat Frontend — Local Dev Config ────────────────────────────────────
#
# Copy this to .env.local (never commit .env.local)
#
# RECOMMENDED: proxy mode — Vite forwards /api/* to the TARS backend.
# No backend CORS changes needed. Works out of the box.
VITE_TARS_API_BASE=/api

# ALTERNATIVE: direct backend mode.
# Requires the TARS backend CORS allowlist to include http://localhost:5173.
# Only use this if you need to bypass the Vite proxy (e.g. testing CORS itself).
# VITE_TARS_API_BASE=http://127.0.0.1:8000

# TARS backend location for the Vite proxy (dev only — server-side, never in bundle)
# The proxy rewrites /api/* → TARS_BACKEND_URL/*
VITE_TARS_BACKEND_URL=http://127.0.0.1:8000

# Operator token for privileged API calls (dev only — injected by Vite proxy, never in browser bundle)
# Required for PATCH /chat/profiles/{actor} in PersonalityOnboarding.
# In production: inject server-side via a BFF/reverse proxy. NEVER hardcode.
VITE_TARS_OPERATOR_TOKEN=
